项目说明：
	   1：.net调用python实质就是开启一个进程（使用Process 类实现），使用本地的python环境运行Python代码（本人基于.netcore 2.2 开发web应用）
       2：上面已说明实质所以对于网上引入第三包的问题，这里自然而然就解决了
	   3：对于网上说的打包成exe文件，本人简单看了一下也没采用原因两点=>
			*本人专项不是pyhton，所以肯定会浪费时间（学习就要有打破砂锅问到底的态度哦）
			*最大一点就是 .exe文件太大，而且运行起来太慢；给用户体验感不是很好
		  所以基于这两点放弃了 .exe文件